/**
 * @NScriptName Hello Scheduled Script
 * @NScriptType ScheduledScript
 * @NApiVersion 2.0
 * */
import {EntryPoints} from "N/types";

export const execute: EntryPoints.Scheduled.execute = (context: EntryPoints.Scheduled.executeContext) => {
  //Todo: Execute Script Here
}